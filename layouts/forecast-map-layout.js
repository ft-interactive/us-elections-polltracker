// provides a dictionary of state colours based on defined thresholds for how safe a bet it is for a given candidate

function forecastMapLayout(stateData) { 
    const layout = {};
    console.log( 'map layout' );
    console.log( stateData );
    return layout;
}

module.exports = forecastMapLayout;